version: "2"
sql:
  - engine: "sqlite"
    schema: "migrations"
    queries: "queries"
    gen:
      go:
        package: "database"
        out: "../internal/database"
        sql_package: "database/sql"
        emit_json_tags: true
        emit_interface: true

